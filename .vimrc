" These are Vim commands that will run every time you start Vim.

" Syntax highlighting
syntax on
" show line numbers
set number
" When pasting from system's clipboard, fix indentation.
set paste
" Modern Vim features
set nocompatible
" Tab formatting
set expandtab
set tabstop=2
set shiftwidth=2
set autoindent
set smartindent
" Set the default shell to bash (if you are using fish, zsh, etc)
set shell=bash
" Enable mouse control for changing tabs, selecting text, scrolling, etc.
set mouse=a
" If indented and line is wider than terminal, also indent softwrapped line.
set breakindent

" That's all. Happy Viming.

